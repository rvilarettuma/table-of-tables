import React, { Component } from 'react';
import { Alert } from 'reactstrap';

export default class App extends Component {

  render() {
    return (
      <div>
        <Alert color="primary">Test Alert</Alert>
      </div>
    );
  }
}
